import React from 'react'
import { View, Text } from 'react-native'
import Button from './Button'

export default function ExpressRental() {
  const handleTap = () => {
  }
  return (
    <Button text="Express Rental" onPress={handleTap} />
  )
}
